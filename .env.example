# Base URL of the AIEC chatbot backend (backend-py), no trailing slash.
# ChatWidget appends /api/chat itself. Example: http://localhost:3001 for
# local dev, or https://api.asian.edu.np once deployed to the VPS.
VITE_CHAT_API_URL=
